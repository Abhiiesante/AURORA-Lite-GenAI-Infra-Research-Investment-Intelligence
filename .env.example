# Root env (optional)

# CI Gates thresholds (optional overrides)
PERF_P95_BUDGET_MS=1500
METRICS_WINDOW_SAMPLES=50
SMAPE_MAX=80
# Per-metric SMAPE override example (use SMAPE_MAX_<METRIC>)
SMAPE_MAX_mentions=65
ERROR_RATE_MAX=0.02
ALLOWED_RAG_DOMAINS=example.com
RAG_MIN_SOURCES=1

# Market Map gate
MARKET_P95_BUDGET_MS=2000

# Main CI toggle to enforce market perf gate (weekly workflow always enforced)
CI_MARKET_GATE=0
