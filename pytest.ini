[pytest]
# Include both application tests and repo-level tests (e.g., Phase 6 KG tests)
testpaths = 
    apps/api/tests
    tests
addopts = -q
markers =
	phase5: Phase 5 tests
    phase6: Phase 6 tests
